import React from 'react';
import UpdatePasswordMarkup from './UpdatePasswordMarkup';

const UpdatePassword = props => {
  return <UpdatePasswordMarkup {...props} />;
};

export default UpdatePassword;
